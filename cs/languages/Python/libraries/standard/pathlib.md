# 概述
面向对象的路径操作(区别于之前的字符串操作)

# 功能
## 获取特定路径 (类方法)
- Path.home()

    返回用户家目录的Path对象

- Path.cwd()

    返回当前目录的Path对象

## 使用指定路径
## 路径组件拆分
## 路径判断
## 路径转换和规范化
- 转换
    - .resolve()

        转绝对路径, 同时解析符号链接, 返回新Path对象

    - .absolute()

        转绝对路径, 不解析符号链接, 返回新Path对象



## 文件/目录操作 (执行IO)
## 目录遍历
## 其他

# 案例
- 模块导入

    ```python
    from pathlib import Path
    ```