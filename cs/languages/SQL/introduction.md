# 概念
- Structured Query Language, 管理和操作关系型数据库的标准化语言
- 有一套标准语法, 不同的数据库会引入一些特有语法

# 最佳实践
## 语法规范
1. 单行或多行书写, 以分号结尾
1. 关键字建议大写(不区分大小写)
1. 使用空格/缩进增加可读性

案例
```SQL
SELECT id, time, result
FROM qc_result
ORDER BY id DESC
```

## 数据库设计
- 列具有原子性, 无重复列
- 非主键完全依赖于主键, 而非对其他非主键的部分依赖
- 非主键之间无依赖传递